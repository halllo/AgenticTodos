{
  "name": "agentic-todos",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "build:sandbox": "esbuild src/sandbox.ts --bundle --platform=browser --outfile=../backend/wwwroot/sandbox.js",
    "start": "npm run build:sandbox && ng serve",
    "dev": "npm run build:sandbox && ng serve",
    "build": "npm run build:sandbox && ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "packageManager": "npm@12.0.1",
  "dependencies": {
    "@ag-ui/client": "^0.0.57",
    "@angular/common": "^22.0.8",
    "@angular/compiler": "^22.0.8",
    "@angular/core": "^22.0.8",
    "@angular/forms": "^22.0.8",
    "@angular/platform-browser": "^22.0.8",
    "@angular/router": "^22.0.8",
    "@mcp-b/global": "^4.0.0",
    "@mcp-b/transports": "^4.0.0",
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "rxjs": "~7.8.2",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@angular/build": "^22.0.9",
    "@angular/cli": "^22.0.9",
    "@angular/compiler-cli": "^22.0.8",
    "esbuild": "latest",
    "jsdom": "^29.1.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
